import { RangeSliderPosition, RangeSliderProps } from './types';
export declare function getCoordinates(e: MouseEvent | TouchEvent): {
    x: number;
    y: number;
};
export declare function getValues(position: RangeSliderPosition, props: RangeSliderProps, rect: ClientRect): {
    x: number;
    y: number;
};
export declare function round(value: number, increment: number): number;
