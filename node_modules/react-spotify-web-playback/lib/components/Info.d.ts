import * as React from 'react';
import { IStylesOptions } from '../types/common';
import { IPlayerTrack } from '../types/spotify';
interface IProps {
    handleFavoriteStatusChange: (status: boolean) => any;
    isActive: boolean;
    showSaveIcon: boolean;
    track: IPlayerTrack;
    token: string;
    styles: IStylesOptions;
    updateSavedStatus?: (fn: (status: boolean) => any) => any;
}
interface IState {
    isSaved: boolean;
}
export default class Info extends React.PureComponent<IProps, IState> {
    private _isMounted;
    constructor(props: IProps);
    componentDidMount(): Promise<void>;
    componentDidUpdate(prevProps: IProps, prevState: IState): Promise<void>;
    componentWillUnmount(): void;
    private handleClickIcon;
    private setStatus;
    private updateState;
    render(): JSX.Element;
}
export {};
